[package]
name = "lau-token-economy"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/SuperInstance/lau-token-economy"
description = "Rust library for token economy in the PLATO ecosystem"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
